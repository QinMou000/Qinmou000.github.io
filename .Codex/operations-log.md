# 操作日志

## 编码前检查 - 博客界面改进
时间：2026-06-06 21:13:31

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-博客界面改进.md`
- [x] 将使用以下可复用组件：
  - 首页筛选数据协议：`themes/hexo-theme-coder 2/layout/index.ejs` - 复用分类筛选和年份分组
  - 主题状态管理：`themes/hexo-theme-coder 2/source/js/js.js` - 复用 `localStorage` 黑白主题状态
  - 文章目录生成：`themes/hexo-theme-coder 2/source/js/js.js` - 复用标题扫描与滚动高亮
  - 全局导航：`themes/hexo-theme-coder 2/layout/_partial/nav.ejs` - 承载首页入口和主题开关
- [x] 将遵循命名约定：CSS 类名使用连字符，运行时钩子使用 `data-*` 属性，JavaScript 使用驼峰命名。
- [x] 将遵循代码风格：EJS 保持现有缩进，Stylus 延续块式 CSS 写法，JavaScript 使用现有 ES5 风格以兼容主题代码。
- [x] 确认不重复造轮子：已检查首页、分类页、文章页、导航、页脚和全局脚本，现有搜索功能已移除，因此本次不伪造搜索入口。

## 技术选型理由
- 为什么用这个方案：在现有静态 HTML 中通过数据属性控制显示状态，可保持 Hexo 构建链路不变。
- 优势：零新增依赖、改动范围小、可渐进增强、分类筛选与加载更多可以共享状态。
- 劣势和风险：所有文章仍存在于生成的 HTML 中，主要优化首屏视觉和交互，不减少 HTML 文件体积。

## 关键风险点
- 并发问题：纯前端同步交互，无共享异步状态。
- 边界条件：筛选结果少于一批、年份分组全部隐藏、文章仅有一个标题或没有标题。
- 性能瓶颈：首页包含 139 篇文章，交互需避免反复进行昂贵 DOM 查询。
- 安全考虑：不拼接用户输入，不引入外部数据提交。

## 实施中检查 - 首页响应式布局
时间：2026-06-06 21:13:31

- [x] 是否使用了摘要中列出的可复用组件：使用首页筛选数据协议和现有响应式样式分文件。
- [x] 命名是否符合项目约定：新增 `home-load-more`、`home-result-summary` 等类名与既有命名一致。
- [x] 代码风格是否一致：EJS、Stylus 和 JavaScript 均沿用相邻实现格式。
- [x] 针对性补充收集：浏览器验证发现横向分类导航的最小内容宽度撑大单列网格。
- [x] 修正方案：为网格子项设置 `min-width: 0`，并将横向滚动限制在宽度为 100% 的分类导航内部。

## 实施中检查 - 文章重复标题
时间：2026-06-06 21:13:31

- [x] 是否使用了摘要中列出的可复用组件：继续在 `post.ejs` 的内容输出边界处理，不修改文章源文件。
- [x] 命名是否符合项目约定：局部变量使用既有驼峰命名。
- [x] 代码风格是否一致：使用 EJS 内现有 JavaScript 预处理方式。
- [x] 针对性补充收集：Hexo 为标题注入空的锚点标签，原精确正则未覆盖该结构。
- [x] 修正方案：提取首个一级标题，剥离内部标签后与页面标题比较，仅在完全相同时移除。
- [x] 多标题技术文章补充验证：标题中的 `&&` 会在模板阶段表现为 HTML 实体，因此在目录生成前增加 DOM 文本比较兜底。

## 编码后声明 - 博客界面改进
时间：2026-06-06 21:18:47

### 1. 复用了以下既有组件
- 首页分类筛选协议：用于分类状态和文章匹配，位于 `themes/hexo-theme-coder 2/layout/index.ejs`。
- 主题状态管理：用于保存黑白主题偏好，位于 `themes/hexo-theme-coder 2/source/js/js.js`。
- 文章目录生成：用于标题扫描、跳转和滚动高亮，位于 `themes/hexo-theme-coder 2/source/js/js.js`。
- 全局导航：用于承载首页入口和主题切换，位于 `themes/hexo-theme-coder 2/layout/_partial/nav.ejs`。

### 2. 遵循了以下项目约定
- 命名约定：新增 `home-load-more`、`home-result-summary`、`theme-toggle`，与 `home-post-link` 等既有连字符类名一致。
- 代码风格：模板使用 EJS，样式继续使用 Stylus 分文件，交互延续原生 DOM 与现有 jQuery。
- 文件组织：首页逻辑留在首页模板，文章逻辑留在文章模板与全局脚本，全局视觉留在样式入口。

### 3. 对比了以下相似实现
- 首页分类筛选：新增渐进展示状态，但继续由同一组文章节点和年份分组派生显示状态。
- 分类页文章树：未复制其文章组织逻辑，首页仍保持适合时间流浏览的年份分组。
- 文章目录：只增加重复标题清理和少标题隐藏，保留原滚动高亮与平滑跳转。

### 4. 未重复造轮子的证明
- 检查了首页、分类页、文章页、导航、页脚、全局脚本和搜索占位模板。
- 仓库搜索功能已明确移除，本次未新增不完整搜索实现。
- 未新增依赖或构建脚本，全部改动基于现有 Hexo、EJS、Stylus 和浏览器 API。

## 回滚方案
- 回滚主题目录下本次修改的九个模板、样式和脚本文件即可恢复旧界面。
- 本次未修改文章内容、站点数据、依赖版本和发布配置，不涉及数据迁移。

## 缺陷修复 - Markdown 公式与文章目录
时间：2026-06-06 21:18:47

- 现象：KaTeX 公式下方显示一份压平后的公式文本；部分仅有一个标题的文章不显示目录。
- 根因：
  - KaTeX CSS 使用外部 CDN，实际页面未加载该样式，导致无障碍 MathML 层可见。
  - 目录条件被改成少于两个标题时隐藏，偏离原有“有标题就显示”的行为。
- 修复：
  - 复用项目已安装的 `katex` 包，将 `katex.min.css` 和字体资源放入主题本地静态资源。
  - 头部改为引用本地 KaTeX CSS，取消外部 CDN 依赖。
  - 增加 MathML 视觉隐藏兜底，保留其无障碍语义。
  - 目录恢复为仅在完全没有标题时隐藏。

## 编码前检查 - CSDN 专栏归档
时间：2026-06-06 22:43:10

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-CSDN专栏归档.md`
- [x] 将使用以下可复用组件：
  - 仓库 Markdown 文章格式：`source/_posts/*.md` - 复用 YAML frontmatter、Markdown 正文和远程图片链接习惯
  - CSDN 专栏列表：`ul.column_article_list` - 获取专栏内完整文章清单
  - CSDN 文章正文：`#content_views` / `.article_content` - 提取并转换正文
- [x] 将遵循命名约定：目录采用专栏原名，文件采用文章标题，元数据字段使用英文标识符。
- [x] 将遵循代码风格：工作记录和说明使用简体中文，生成文档使用 UTF-8 无 BOM，Markdown 结构保持清晰。
- [x] 确认不重复造轮子，证明：已检查 `scripts/`、`package.json`、`source/_posts/`，仓库没有 CSDN 抓取器或 HTML 转 Markdown 工具。

## 技术选型理由
- 为什么用这个方案：从公开专栏页获取权威清单，再逐篇解析正文，可以核对总数并保留来源。
- 优势：不需要登录态，不新增依赖，输出独立于 Hexo 内容目录，可重复核验。
- 劣势和风险：页面结构变化会影响提取；复杂富文本组件可能需要保留原始 HTML。

## 关键风险点
- 并发问题：采用有限并发和失败重试，避免高频访问。
- 边界条件：处理非法文件名字符、重复标题、空正文、代码语言缺失、表格换行。
- 性能瓶颈：主要是 26 次文章网络请求，转换本身为线性复杂度。
- 安全考虑：只读取公开页面，不执行脚本，不读取 Cookie，不发送用户数据。

## 实施中检查 - CSDN 专栏归档
时间：2026-06-06 22:47:36

- [x] 是否使用了摘要中列出的可复用组件：使用仓库 Markdown 格式、CSDN 专栏文章列表和文章正文容器。
- [x] 命名是否符合项目约定：目录使用 `MySQL`、`C++`，文件名使用文章标题，元数据使用统一英文标识符。
- [x] 代码风格是否一致：生成内容为 UTF-8 Markdown，说明文字与记录均使用简体中文。
- [x] 针对性补充收集：发现《C++进阶：实现红黑树》正文较短后重新读取公开页面，确认 CSDN 返回的正文自身在同一位置结束。

## 编码后声明 - CSDN 专栏归档
时间：2026-06-06 22:47:36

### 1. 复用了以下既有组件
- 仓库 Markdown 文章格式：用于 YAML frontmatter、正文层级、代码围栏和远程图片链接，位于 `source/_posts/*.md`。
- CSDN 专栏列表结构：用于获取完整文章清单，位于两个专栏页的 `ul.column_article_list`。
- CSDN 正文结构：用于提取标题和正文，位于文章页的 `h1#articleContentId` 与 `#content_views`。

### 2. 遵循了以下项目约定
- 命名约定：输出目录采用专栏原名，文件采用文章标题，与内容可读性要求一致。
- 代码风格：Markdown 使用 YAML frontmatter 和标准围栏代码块，说明性文本使用简体中文。
- 文件组织：归档文件集中在项目根目录 `csdn/`，工作记录集中在 `.Codex/`。

### 3. 对比了以下相似实现
- `source/_posts/2024年终总结.md`：沿用 frontmatter 与远程图片写法，但归档文件增加来源与 CSDN ID。
- `source/_posts/2025.10.22随笔：大学幸福生活.md`：沿用正文图片链接，不下载图片二进制。
- `source/_posts/2025.10.28随笔：皮质醇爆炸后的思考.md`：沿用 Markdown 与内嵌 HTML 可共存的策略。

### 4. 未重复造轮子的证明
- 检查了 `scripts/`、`package.json` 和 `source/_posts/`，确认不存在 CSDN 专栏抓取器。
- 本次没有新增项目依赖或构建脚本，只生成用户要求的归档文档和清单。

## 回滚方案
- 删除本次新增的 `csdn/` 与 `.Codex/context-summary-CSDN专栏归档.md`、`.Codex/verification-report-CSDN专栏归档.md`。
- 从 `.Codex/operations-log.md` 移除本任务对应记录。
- 本次没有修改 Hexo 内容、主题、依赖或配置，不涉及数据迁移。

## 编码前检查 - CSDN 文章格式统一
时间：2026-06-06 23:33:42

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-CSDN专栏归档.md`
- [x] 将使用以下可复用组件：
  - 随笔 frontmatter：`source/_posts/2025.10.22随笔：大学幸福生活.md` - 复用 `title/date/categories` 结构
  - C++ frontmatter：`source/_posts/C++：C++11并发支持库.md` - 复用技术分类写法
  - CSDN 归档清单：`csdn/manifest.json` - 在精简文章 frontmatter 后继续保存文章 ID 和原文链接
- [x] 将遵循命名约定：标题保持文章原名，日期使用 `YYYY-MM-DD`，分类使用 `MySQL` 或 `C++`。
- [x] 将遵循代码风格：YAML 不为普通中文标题和日期添加多余引号，分类使用两行列表格式。
- [x] 确认不重复造轮子，证明：直接按现有 `source/_posts/` frontmatter 结构执行机械转换，不新增解析器或依赖。

## 技术选型理由
- 为什么用这个方案：归档文章采用与 Hexo 文章一致的最小 frontmatter，可直接复制到 `source/_posts/` 使用。
- 优势：格式统一、字段简单、分类可被 Hexo 正确识别。
- 劣势和风险：文章文件内不再直接显示 CSDN ID 和原文链接，但 `manifest.json` 仍保留完整映射。

## 实施中检查 - CSDN 文章格式统一
时间：2026-06-06 23:36:20

- [x] 是否使用了摘要中列出的可复用组件：使用 `source/_posts/` 的 `title/date/categories` 结构。
- [x] 命名是否符合项目约定：标题保持原文，日期统一为 `YYYY-MM-DD`，分类与专栏目录一致。
- [x] 代码风格是否一致：frontmatter 后保留一个空行，正文不做内容改写。
- [x] 异常修正：补充空行时曾写入字面量换行转义，抽查后立即修复，并重新验证全部 26 篇文章。
- [x] 校验规则修正：SQL 字段 `` `name` `` 曾被宽泛规则误判，收紧规则后最终检查通过。

## 编码后声明 - CSDN 文章格式统一
时间：2026-06-06 23:36:20

### 1. 复用了以下既有组件
- `source/_posts/2025.10.22随笔：大学幸福生活.md`：复用标准三字段 frontmatter。
- `source/_posts/C++：C++11并发支持库.md`：复用技术文章分类列表写法。
- `csdn/manifest.json`：继续保存 26 篇文章的 CSDN ID 和原文链接。

### 2. 遵循了以下项目约定
- 命名约定：标题不加多余引号，日期精确到天。
- 代码风格：分类使用 `categories` 列表，frontmatter 后保留空行。
- 文件组织：文章仍按 `csdn/MySQL/` 和 `csdn/C++/` 分类存放。

### 3. 对比了以下相似实现
- 随笔文章：字段结构完全一致，仅分类值不同。
- C++ 技术文章：分类写法完全一致，标题保持 CSDN 原标题。
- 现有 CSDN 归档：仅替换 frontmatter，正文、文件名和目录保持不变。

### 4. 未重复造轮子的证明
- 采用机械字段转换，没有新增依赖、脚本或抽象层。
- 来源信息已由现有 `manifest.json` 统一维护，没有在文章中复制一套新格式。

## 编码前检查 - 首页分类入口调整
时间：2026-06-07 11:42:01

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-首页分类入口调整.md`
- [x] 将使用以下可复用组件：
  - 独立分类总览：`themes/hexo-theme-coder 2/layout/categories.ejs` - 统一承担分类浏览
  - 全局导航：`themes/hexo-theme-coder 2/layout/_partial/nav.ejs` - 复用现有“分类”入口
  - 首页年份分组：`themes/hexo-theme-coder 2/layout/index.ejs` - 保留时间流结构
  - 首页渐进展示：`themes/hexo-theme-coder 2/layout/index.ejs` - 保留每批 24 篇和加载更多
- [x] 将遵循命名约定：CSS 类名使用连字符，运行时钩子使用 `data-*`，JavaScript 使用驼峰命名。
- [x] 将遵循代码风格：EJS 保持现有缩进，Stylus 延续块式写法，脚本保持浏览器原生 ES5 风格。
- [x] 确认不重复造轮子：已检查首页、分类页、导航和文章页；独立分类页与入口均已存在，只需移除首页重复筛选层。

## 技术选型理由
- 为什么用这个方案：首页专注按时间浏览，分类页专注按主题浏览，避免两套分类交互重复维护。
- 优势：交互路径明确、删除故障按钮、减少首页脚本与样式、无需新增依赖。
- 劣势和风险：用户不能在首页原地筛选，需要通过顶部导航进入分类页。

## 关键风险点
- 并发问题：纯静态页面与同步 DOM 操作，无并发共享状态。
- 边界条件：文章数少于 24 篇、没有文章、跨年份批次显示。
- 性能瓶颈：首页静态 HTML 仍包含全部文章，本次只控制可见数量。
- 安全考虑：不处理用户输入，不新增外部请求。

## 实施中检查 - 首页分类入口调整
时间：2026-06-07 11:45:27

- [x] 是否使用了摘要中列出的可复用组件：保留独立分类页、全局导航、首页年份分组和渐进展示。
- [x] 命名是否符合项目约定：保留 `home-posts-panel`、`home-load-more` 和 `data-home-post` 等既有命名。
- [x] 代码风格是否一致：EJS、Stylus 和 JavaScript 均延续相邻实现格式。
- [x] 清理结果：删除首页分类聚合、筛选按钮、筛选状态、数据属性和对应深浅色及响应式样式。
- [x] 构建补偿：`npm run clean` 因 `db.json` 被占用返回 `EPERM`，改用 `npx hexo generate --force` 完成全量生成。

## 编码后声明 - 首页分类入口调整
时间：2026-06-07 11:45:27

### 1. 复用了以下既有组件
- 独立分类页：用于按一级分类、子分类和文章浏览，位于 `themes/hexo-theme-coder 2/layout/categories.ejs`。
- 全局导航：用于提供“分类”入口，位于 `themes/hexo-theme-coder 2/layout/_partial/nav.ejs`。
- 首页年份分组：用于保持按时间浏览，位于 `themes/hexo-theme-coder 2/layout/index.ejs`。
- 首页渐进展示：用于每批显示 24 篇文章，位于 `themes/hexo-theme-coder 2/layout/index.ejs`。

### 2. 遵循了以下项目约定
- 命名约定：保留现有连字符 CSS 类名、`data-*` 交互钩子和驼峰 JavaScript 变量。
- 代码风格：模板使用 EJS，样式使用 Stylus，运行时使用浏览器原生 DOM API。
- 文件组织：首页逻辑留在首页模板，分类逻辑继续由独立分类页承担。

### 3. 对比了以下相似实现
- 首页原筛选实现：删除分类按钮和筛选状态，仅保留与分类无关的渐进展示。
- 独立分类页：保持树状组织和分类归档链接，不复制到首页。
- 文章页分类信息：与首页一致保留分类文本，但分类浏览统一从导航进入。

### 4. 未重复造轮子的证明
- 检查了首页、分类页、导航、文章页和主题配置。
- 仓库已有 `/categories/` 页面和菜单入口，因此没有创建第二个分类页面或新增路由。
- 未新增依赖、构建脚本或客户端框架。

## 回滚方案
- 回滚 `themes/hexo-theme-coder 2/layout/index.ejs` 与 `themes/hexo-theme-coder 2/source/css/_partial/index.styl` 即可恢复首页筛选栏。
- 本次未修改文章、分类数据、主题菜单和发布配置，不涉及数据迁移。

## 编码前检查 - 分类卡片网格
时间：2026-06-07 12:29:14

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-分类卡片网格.md`
- [x] 将使用以下可复用组件：
  - 分类数据聚合：`themes/hexo-theme-coder 2/layout/categories.ejs` - 复用分类路径、数量和文章集合
  - 首页文章信息层级：`themes/hexo-theme-coder 2/layout/index.ejs` - 复用标题与日期的紧凑展示
  - 全局导航：`themes/hexo-theme-coder 2/layout/_partial/nav.ejs` - 保持分类总览入口不变
  - 分类归档生成器：`hexo-generator-category` - 承担完整文章列表
- [x] 将遵循命名约定：新增类名使用 `category-card-*` 和 `categories-*` 连字符格式。
- [x] 将遵循代码风格：EJS 使用现有 ES5 写法，Stylus 延续块式 CSS，响应式断点沿用 860px 和 640px。
- [x] 确认不重复造轮子：已检查分类页、首页、文章页和导航；现有分类归档已经能承载完整列表。

## 技术选型理由
- 为什么用这个方案：卡片网格适合 9 个分类的全局扫描，近期文章提供内容线索，完整归档链接避免总览页过长。
- 优势：桌面信息密度高、移动端自然降级、无需 JavaScript、完整分类路径仍可访问。
- 劣势和风险：总览页不再直接展示全部文章，用户查看旧文章需要进入分类归档。

## 关键风险点
- 并发问题：纯静态模板，无并发状态。
- 边界条件：没有二级分类、文章少于 10 篇、标题过长、分类数量变化。
- 性能瓶颈：模板对每篇文章做一次线性聚合，当前 165 篇规模可忽略。
- 安全考虑：仅输出 Hexo 已解析的站点内容，不接收用户输入。

## 实施中检查 - 分类卡片网格
时间：2026-06-07 12:29:14

- [x] 是否使用了摘要中列出的可复用组件：复用分类聚合、归档路径、首页文章信息层级和全局导航。
- [x] 命名是否符合项目约定：新增类名统一使用 `categories-*` 与 `category-card-*`。
- [x] 代码风格是否一致：EJS 使用 ES5 数组方法，Stylus 沿用块式 CSS 和现有断点。
- [x] 清理结果：删除旧树状页面结构、样式和不再使用的直接文章及子分类文章缓存字段。
- [x] 外部资料用途：仅用于比较标签页、折叠面板和独立导航的适用边界，没有引入外部代码。

## 编码后声明 - 分类卡片网格
时间：2026-06-07 12:29:14

### 1. 复用了以下既有组件
- 分类数据聚合：用于生成分类名称、数量和归档路径，位于 `themes/hexo-theme-coder 2/layout/categories.ejs`。
- 首页文章信息层级：用于卡片内近期文章的标题与日期布局，位于 `themes/hexo-theme-coder 2/layout/index.ejs`。
- 全局导航：继续提供分类总览入口，位于 `themes/hexo-theme-coder 2/layout/_partial/nav.ejs`。
- Hexo 分类归档：继续承担每个分类的完整文章列表。

### 2. 遵循了以下项目约定
- 命名约定：使用 `categories-grid`、`category-card-post` 等连字符类名。
- 代码风格：数据处理留在 EJS，视觉规则留在 Stylus，不新增客户端脚本。
- 文件组织：只修改分类模板和现有首页样式分文件。

### 3. 对比了以下相似实现
- 原树状分类页：从全部文章展开改为分类概览与近期文章，完整内容转交归档页。
- 首页时间流：复用标题与日期层级，但分类卡按主题而非年份组织。
- 文章页分类元数据：继续保持分类名称简洁，二级分类通过标签入口表达。

### 4. 未重复造轮子的证明
- 检查了分类页、首页、文章页、导航和 Hexo 分类生成器。
- 没有新建第二套归档、分页或筛选逻辑。
- 未新增依赖、路由、JavaScript 或构建脚本。

## 回滚方案
- 回滚 `themes/hexo-theme-coder 2/layout/categories.ejs` 和对应的 `index.styl` 分类样式即可恢复树状列表。
- 本次未修改文章分类数据和归档路径，不涉及数据迁移。

## 分类卡片文章上限调整
时间：2026-06-07 12:38:26

- 经用户澄清，分类卡片恢复最多显示 5 篇近期文章。
- 最终交互调整为：分类归档关闭静态分页，一次生成全部文章。
- 复用首页现有“加载更多”，每次显示 24 篇，直到显示该分类全部内容。
- 使用 `hexo-generator-category` 官方 `category_generator.per_page: 0` 配置，不新增客户端数据请求。

## 编码前检查 - 分类归档加载更多
时间：2026-06-07 12:48:42

- [x] 已查阅上下文摘要文件：`.Codex/context-summary-分类归档加载更多.md`
- [x] 将使用以下可复用组件：
  - `themes/hexo-theme-coder 2/layout/index.ejs`：复用首页每批 24 篇和加载更多逻辑。
  - `themes/hexo-theme-coder 2/layout/categories.ejs`：保持分类卡片最多显示 5 篇。
  - `hexo-generator-category`：通过 `per_page: 0` 关闭分类静态分页。
- [x] 将遵循命名约定：沿用 `home-posts-panel`、`home-load-more`、`data-home-post`。
- [x] 将遵循代码风格：EJS 模板、原生 JavaScript 和 Stylus，不新增框架。
- [x] 确认不重复造轮子：已检查首页渐进展示、分类总览、分类生成器和旧分页样式。

### 技术选型理由
- 为什么用这个方案：分类归档本来就使用 `index.ejs`，关闭生成器分页后可直接复用首页加载更多。
- 优势：零新增依赖、无额外请求、首页和分类页行为一致。
- 劣势和风险：分类文章全部写入单个 HTML；当前最大分类规模较小，可接受。

### 关键风险点
- 并发问题：纯静态页面无共享写入，不涉及并发。
- 边界条件：不足 24 篇时按钮会自动隐藏；超过 24 篇时按 24 篇递增。
- 性能瓶颈：超大分类会增大单页 HTML，目前 46 篇的最大分类不会造成明显负担。
- 安全考虑：只切换已有 DOM 的 `hidden` 状态，不拼接或执行文章内容。

## 编码后声明 - 分类归档加载更多
时间：2026-06-07 12:55:00

### 1. 复用了以下既有组件
- `index.ejs` 的文章分组和加载更多逻辑：同时服务首页与分类归档。
- `categories.ejs` 的分类数据：用于卡片内最多 5 篇预览。
- Hexo 分类生成器：用于输出无静态分页的完整分类文章集合。

### 2. 遵循了以下项目约定
- 命名约定：继续使用既有 `home-*` 和 `data-home-*` 命名。
- 代码风格：使用项目已有 EJS、原生 JavaScript 与 Stylus。
- 文件组织：站点生成配置位于 `_config.yml`，展示逻辑保留在主题目录。

### 3. 对比了以下相似实现
- 首页加载更多：分类页完全复用相同批次和结果摘要逻辑。
- 旧分类分页：改为单页完整数据，移除页码、上一页和下一页。
- 分类总览卡片：继续限制为 5 篇，不受归档页变化影响。

### 4. 未重复造轮子的证明
- 检查了首页模板、分类总览模板、分类生成器文档与实现、生成后的分类 HTML。
- 没有新增数据接口、分页组件、前端依赖或构建脚本。

## 回滚方案 - 分类归档加载更多
- 删除 `_config.yml` 中的 `category_generator` 配置即可恢复 Hexo 默认分类分页。
- 如需恢复分页导航，可回退 `index.ejs` 的分页分支和对应样式。
- 本次不修改文章数据与分类路径，无数据迁移。

## 本地预览旧路由修复
时间：2026-06-07

- 直接请求 `http://127.0.0.1:4000/categories/essay/`，旧预览进程只返回 10 篇文章。
- 磁盘中的新生成页面已包含 46 篇，确认问题来自 Hexo Server 启动时缓存的旧分类路由。
- 重启本地预览服务，使其重新读取 `category_generator.per_page: 0`。
- 重启后同一地址返回 46 篇文章，包含每批 24 篇的加载更多逻辑，且没有分页导航。
