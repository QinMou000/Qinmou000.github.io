# 验证报告

## 验证范围
- Hexo 静态构建。
- 首页桌面端和移动端布局。
- 分类筛选与渐进展示。
- 文章标题、目录、主题切换和页脚。

## 自动验证命令
```powershell
npm run build
```

## 补充验证
- 使用本地 `npm run server -- -p 4000` 预览。
- 浏览器检查 1280×720 桌面端和 390×844 移动端。

## 当前状态
通过。

## 测试能力说明
- 仓库没有 lint 脚本和单元测试框架。
- 补偿方案：执行完整静态构建，并通过浏览器检查关键交互和响应式尺寸。

## 验证结果
- `npm run build`：通过，Hexo 静态文件生成成功。
- `git diff --check`：通过，无空白错误；仅显示仓库现有 Windows 换行提示。
- 桌面首页 1280×720：
  - 首批显示 24 / 139 篇。
  - 点击加载更多后显示 48 / 139 篇。
  - 筛选“随笔”后显示 24 / 46 篇。
  - 首页高度从约 11914px 降至约 5614px。
- 移动首页 390×844：
  - 页面内容宽度 375px，无横向页面溢出。
  - 分类导航内部可横向滚动。
  - 分类侧栏和文章区域宽度均约 323px，不再出现 71px 的文章窄列。
- 文章页：
  - 普通标题文章重复标题数量为 0。
  - 含 `&&` 的技术文章重复标题数量为 0。
  - 多标题文章正文 34 个标题生成 34 个目录项，桌面目录正常显示。
  - 标题不足 2 个时目录隐藏。
- 主题切换：
  - 按钮唯一且可点击。
  - 深浅主题切换后 `body` 类名、按钮图标和无障碍标签同步变化。
- 页脚：
  - 显示 `© 2026 WangQin · Theme By Coder`。

## 剩余风险
- 渐进展示只减少首屏可见内容，不减少首页静态 HTML 体积。
- 仓库没有自动化前端测试框架，后续若继续扩大交互功能，建议再引入现有构建体系可接受的浏览器回归测试。

## Markdown 公式与目录回归验证
- `npm run build`：通过，本地 KaTeX CSS 和 60 个字体文件成功生成到 `public/libs/katex/`。
- 目标文章《一次标定+LiDAR缺陷面积测量技术文档》：
  - 页面加载 `/libs/katex/katex.min.css`，不再依赖外部 CDN。
  - KaTeX 使用 `KaTeX_Main` 字体。
  - `.katex-mathml` 为绝对定位的 1px 隐藏层，不再显示重复公式文本。
  - 深色和浅色主题下公式均只显示一份。
  - 正文 5 个标题生成 5 个目录项，目录在桌面端正常显示。
- 目录显示规则恢复为：没有标题时隐藏，只要存在标题就生成目录。

## 首页分类入口调整验证

### 自动验证命令
```powershell
npm run build
npx hexo generate --force
git diff --check
```

### 验证结果
- `npm run build`：通过，首页、分类总览和各分类归档成功生成。
- `npm run clean`：未通过，Windows 返回 `EPERM`，原因是 `db.json` 被现有 Node 进程占用。
- `npx hexo generate --force`：通过，451 个文件完成全量生成，补偿了无法清理缓存的问题。
- `git diff --check`：通过，无空白错误；仅有仓库现有换行转换提示。
- 首页生成结果：
  - 共 165 篇文章。
  - `data-page-size` 为 24，“加载更多”结构和样式存在。
  - 不再包含 `data-category-filter`、`home-category-sidebar` 等筛选结构。
- 分类页生成结果：
  - 共 9 个一级分类分组。
  - 列出全部 165 篇文章。
  - 顶部导航仍包含 `/categories/` 入口。
- 生成 CSS：
  - 不再包含 `home-category-sidebar` 和 `home-category-link`。
  - 保留 `home-load-more` 样式。

### 测试能力说明
- 仓库没有 lint、单元测试或浏览器自动化测试框架。
- 补偿方案：执行 Hexo 普通构建、强制全量构建、生成 HTML 结构断言和生成 CSS 残留检查。

## 分类卡片网格验证

### 自动验证命令
```powershell
npx hexo generate --force
npm run build
git diff --check
```

### 验证结果
- `npx hexo generate --force`：通过，451 个文件完成全量生成。
- `npm run build`：通过，增量构建无错误。
- `git diff --check`：通过，无空白错误；仅有仓库现有换行转换提示。
- 分类页面结构：
  - 生成 9 张一级分类卡片和 9 个完整归档入口。
  - 生成 5 个二级分类入口。
  - 每张卡最多显示 5 篇近期文章。
  - 旧 `category-tree` 和 `category-branch` 结构不存在。
  - 分类按文章数量降序排列。
- 响应式样式：
  - 桌面端 3 列。
  - 1100px 以下 2 列。
  - 640px 以下 1 列。
  - 深色和浅色主题均包含完整卡片样式。

### 剩余风险
- 仓库没有浏览器自动化测试框架，本次视觉验证以生成 HTML 和 CSS 结构断言补偿。
- 文章标题在卡片中使用单行省略，完整标题可在进入文章或分类归档后查看。

## 分类归档加载更多验证

### 验证结果
- 分类总览卡片最多显示 5 篇文章，没有卡片超过上限。
- 首页生成 165 篇文章，保留每批 24 篇的“加载更多”，没有静态分页导航。
- 分类生成器使用 `category_generator.per_page: 0`，隔离目录全量生成通过，共生成 439 个文件。
- “随笔”分类：
  - 单个 HTML 包含全部 46 篇文章。
  - 包含“加载更多”和 `data-page-size="24"`。
  - 不包含页码、上一页或下一页导航。
- “C”分类单个 HTML 包含全部 25 篇文章，可在首批 24 篇后加载剩余 1 篇。
- “Qt”分类只有 1 篇；首次渲染后按钮会自动隐藏。
- 隔离生成目录中没有任何分类 `page/2` 目录。
- `git diff --check`：通过，无空白错误；仅有仓库现有换行转换提示。

### 构建说明
- 常规输出目录存在被运行中的 Node 进程锁定的旧分页文件，Hexo 清理旧文件时出现 `EPERM`。
- 使用同一站点配置将 `public_dir` 临时指向隔离目录后，全量生成成功，验证了本次模板和配置。

### 剩余风险
- 仓库没有浏览器自动化测试框架，本次通过生成 HTML 结构断言和脚本逻辑检查补偿。
- 分类页把该分类全部文章写入一个 HTML；当前最大分类为 46 篇，体积风险较低。

### 本地预览验证
- 重启前请求 `http://127.0.0.1:4000/categories/essay/`：返回 10 篇，证明旧预览进程仍在使用启动时的分页路由。
- 重启 Hexo Server 后再次请求：
  - HTTP 状态为 200。
  - 页面包含全部 46 篇文章。
  - 包含“加载更多”和 `data-page-size="24"`。
  - 不包含静态分页导航。
