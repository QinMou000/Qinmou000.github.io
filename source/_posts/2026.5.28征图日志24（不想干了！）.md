---
title: 2026.05.28征图日记24（不想干了！）
date: 2026-05-28
categories:
  - 征图日记
---

# 2026.05.28征图日记24（不想干了！）

上午qxx说昨天标的凹坑缺陷标的不好一共67张上午返工又标了一下，标完了之后qxx就说其他人还没标好，让我自己玩一会，准备和chatgpt聊一下，如何能让财富跑赢通胀？

今天一边在标注，一边在思考很多事情，这个公司我到底选对了吗？我当初是不是应该去其他地方，感觉我被耍了，进来说是干agent开发，结果第一个月让我做ios，现在有让我干标注。mentor也对我不上心，让我自己看代码，我去问他的时候他又说目前agent没啥困难，但实际上我看代码并且测试之后也发现是纸糊的，python管理杂乱，说是agent，不如说是一个会调用工具的chatbot，只有基本的会话管理，没有上下文管理，功能也就是基本能跑通，mentor和别的同事聊天的时候被我听到说，还不如把这个项目砍了。说没什么意思，感觉不能落地。而且agent是基于本来的django框架，厚重，并且数据库用的是postgreSQL，并不好迁移。

下午qxx有给我452张任务，leader说一天标400张，她一次性就给我这么多，我不管，标完距离下班还有一个半小时，我要开始写力扣了～

## 接雨水

```c++
class Solution {
public:
    int trap(vector<int>& height) {
        int pillar = 0; // 柱子面积
        int max_h = 0;
        for (auto e : height) {
            if (e > max_h)
                max_h = e;
            pillar += e;
        }
        int left = 0, right = height.size() - 1; // 左闭右闭
        int pillar_and_water = 0;                // 柱子和水的面积
        int H = 1;
        while (left <= right && H <= max_h) {
            while (height[left] < H)
                left++;
            while (height[right] < H)
                right--;
            pillar_and_water += right - left + 1;
            H++;
        }
        return pillar_and_water - pillar;
    }
};
```

## 找到字符串中的所有字母异位词

```c++
class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        unordered_map<char, int> cnt1;
        unordered_map<char, int> cnt2;
        for (char c : p)
            cnt2[c]++;
        vector<int> ans;

        for (int left = 0, right = 0; right < s.size(); right++) {
            // 入窗口
            cnt1[s[right]]++;
            // 判断
            while (right - left + 1 > p.size()) {
                // 出窗口
                if (--cnt1[s[left]] == 0)
                    cnt1.erase(s[left]); // 因为是哈希表，需要删除
                left++;
            }
            // 更新结果
            if (cnt1 == cnt2)
                ans.push_back(left);
        }
        return ans;
    }
};
```

btw:还是要多复习前面写过的东西，很多都忘了，今天连滑动窗口的模板都不记得了，准备重刷一边hot100！

---

今日工作内容
1) 标注凹坑67，包膜脏污26，褶皱48，膜面气泡452
2) 测试webAi的agent，跑通流程

下阶段计划
1) 继续梳理webAi代码

---