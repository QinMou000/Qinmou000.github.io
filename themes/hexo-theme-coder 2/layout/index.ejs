<div class="hd logodiv">
    <%- image_tag(theme.gender,{class: 'logopng', alt: 'logo'}) %>
    <p class="logot">
         <%= theme.logo_title %> 
         <span class="logo_2"></span>
        <h3 class="words" style="font-weight: bold;font-family: Georgia;"> 
            <%= theme.words %> 
        </h3>
    </p>
</div>

<% if (page.total > 1) { %>
<div class="pagination">
    <% if (page.prev) { %>
        <a class="page-prev" href="<%- url_for(page.prev.path) %>">上一页</a>
    <% } %>

    <% for (var i = 1; i <= page.total; i++) { %>
        <% var link = i === 1 ? url_for('/') : url_for(config.pagination_dir + '/' + i + '/'); %>
        <a class="page-num <%= page.current === i ? 'current' : '' %>" href="<%- link %>"><%= i %></a>
    <% } %>

    <% if (page.next) { %>
        <a class="page-next" href="<%- url_for(page.next.path) %>">下一页</a>
    <% } %>
</div>
<% } %>

<%- partial('_partial/nav') %>
<div class="hd">
    <% var last; %>
    <% page.posts.each(function (post) { %>
        <% var year = post.date.year(); %>
        <% if (last != year){ %>
            <div class="titlex">
                <p><span class="jing"></span><span style="color: #6d9ee7;"> 公元</span> 
                 <%= year %>年 🕛
                </p>
            </div>
            <% last = year; %>
        <% } %>
        <a class="flink" href="<%- url_for(post.path) %>">
            <div class="list">
                <div class="postname">
                    <%= post.title %>
                </div>
                <div class="posttime">
                    <%- full_date(post.date, 'MMMM Do') %>
                </div>
            </div>
        </a>
    <% }) %>
</div>
